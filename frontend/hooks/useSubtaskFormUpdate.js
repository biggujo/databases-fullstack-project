import useSubtaskForm from './useSubtaskForm.js';

const useSubtaskFormUpdate = (
  operation, initialValues, onSubmit) => useSubtaskForm(operation,
  initialValues,
  onSubmit,
);

export default useSubtaskFormUpdate;
